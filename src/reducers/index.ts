import { combineReducers } from 'redux';

function pageData(state = {}, action): any {
    switch (action.type) {
        default:
            return state
    }
}

export const reducers = combineReducers({
    pageData
});
